import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  container: {
    marginTop: 50,
  },

  buttonStyle: {
    marginLeft: 10,
    height: 55,
    fontWeight: 'bold',
  },

  containerCard: {
    marginTop: 50,
  },

  errorStyle: {
    color: '#f00',
    fontWeight: 'bold',
  },
}));
